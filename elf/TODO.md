1,观察 相互调用后的 plt got
2,所以，也就是说，由于 got plt 与 text段引用位置相对固定的情况下，为了解决无法进行动态内存映射的CPU上内存分布的问题，我们需要ldscript保证 text段与got plt段临近

需要实验
extern解析
它表示是外部的导入函数，就是这个so文件中需要使用到的外部的函数。


1，命名空间实验
2，类内部类实验
3, 文件间变量实验
4, 文件间函数实验
5，库间变量实验
6，库间函数实验